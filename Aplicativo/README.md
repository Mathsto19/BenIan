# BenIan - Aplicativo

Esta pasta fica reservada para a versão empacotada do BenIan.

Enquanto o executável não for gerado, use:

```powershell
python ..\BenIan.py
```

Para gerar o executável:

```powershell
..\scripts\build_exe.ps1
```

A saída esperada do build é:

```text
dist/BenIan.exe
```
